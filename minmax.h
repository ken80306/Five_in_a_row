#include<cstdlib>

temlpate<class GAME>
void minmax(GAME state,int depth)
{
	
	// next move

	for()
		minmax(nexr_state,depth-1);
	class 

	for() // the other
		minmax(next_...,depth-1);
	for() // self
		minmax(next_state,depth-1);


}
